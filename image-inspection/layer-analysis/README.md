# layer-analysis

Inspection of real images using `docker image history`, `docker image inspect`, and `docker save`. No custom builds required. All exercises use public images.

---

## Run the exercises

```bash
bash exercises.sh
```

Run each block individually.

---

## Prerequisites

```bash
docker pull nginx:alpine
docker pull python:3.13-slim
docker pull python:3.13-alpine
```
